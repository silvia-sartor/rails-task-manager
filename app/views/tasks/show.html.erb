<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @mex %></p>
<p><%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
      </p>
<p><%= link_to "Edit", edit_task_path(@task),
method: :get %>
</p>
<%= link_to "Back to my tasks", tasks_path, method: :get %>
